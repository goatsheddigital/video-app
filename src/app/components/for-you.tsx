import { getServers } from "dns";
import { agent, agent as _client } from "~/lib/api";


export default async function ForYou() {

    // authenticate user
    var user = getServers();

    console.log(user);
    
     // get user's popublar feeds


    // let {data: {feeds}} = await agent.app.bsky.unspecced.getPopularFeedGenerators({



  return (
    
    
    <div>

    </div>
  );
}